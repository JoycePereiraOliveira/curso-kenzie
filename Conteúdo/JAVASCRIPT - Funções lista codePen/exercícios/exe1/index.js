// Crie uma função chamada areOlder que recebe a idade de uma pessoa e retorna um boolean (true ou false) se a pessoa é de maior ou não.

function areOlder(age){
    return age >= 18;
  }
  
  console.log(areOlder(18));